# API base URL. In dev the Vite proxy forwards /api -> http://localhost:4000,
# so the default works out of the box.
VITE_API_BASE=/api

# IVAO OAuth (production SSO). Leave blank to use dev login locally.
VITE_IVAO_CLIENT_ID=
VITE_IVAO_REDIRECT_URI=http://localhost:5173/login/callback
